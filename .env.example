GRADIENT_RUNS_DIR=runs
GRADIENT_WORKER_DIR=worker-workspace
GRADIENT_CODEX_COMMAND=codex
GRADIENT_INFERENCE_URL=https://api.pinference.ai/api/v1
GRADIENT_INFERENCE_API_KEY=
GRADIENT_MODEL=Qwen/Qwen3.5-2B
GRADIENT_SANDBOX_IMAGE=gradient-sandbox:local
# Required for non-loopback API clients. Localhost is allowed without a token.
GRADIENT_API_TOKEN=
# Configure Prime authentication with prime login; cloud sandbox SDK uses PRIME_API_KEY.
